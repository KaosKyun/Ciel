---
description: "Analyze recent sessions to detect failure modes and propose skill improvements"
---

Load the `ciel` skill via `skill({ name: "ciel" })` and follow the `ciel-improve` instructions.

User input:
```
$ARGUMENTS
```
