---
description: "Scan Ciel skills for stale references (dead URLs, outdated versions) and propose updates"
---

Scan every Ciel skill for stale external references and produce a freshness patch-set.

Usage: `/ciel-refresh [scope] [--force]`

Load the `ciel` skill via `skill({ name: "ciel" })` and follow the freshness audit instructions.

Args: $ARGUMENTS
