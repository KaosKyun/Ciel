# Local environment / config — never commit
.env
.env.local
.env.*.local

# macOS
.DS_Store

# Editor / IDE
.vscode/
.idea/

# Temp
*.tmp
*.swp
*~

# Ciel local manifest snapshots (for debugging)
.ciel-manifest.debug.json

# Project-level overlay created by install.sh (per-machine, not source of truth)
ciel-overlay.md

# Project-level Claude Code config created by /ciel-init (per-machine)
# settings.json contains absolute $CIEL_DIR hook paths specific to this machine
.claude/settings.json
.claude/settings.json.bak-*
